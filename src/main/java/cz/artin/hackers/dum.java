package cz.artin.hackers;

import org.bukkit.Location;
import org.bukkit.plugin.java.JavaPlugin;

import static org.bukkit.Material.*;

public class dum extends JavaPlugin {
    public static void Dum(Location lokace) {
        lokace.add(6, -1, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }

        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -5);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }

        lokace.add(-10, -3, 4);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, 1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -5);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(9, -3, -1);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(8, -3, 10);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(-1, -3, 0);
        for (int i = 1; i < 4; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, -3, -10);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(11, 0, 1);
        for (int i = 1; i < 12; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, -5);
        for (int i = 1; i < 3; i++) {
            lokace.add(0, 1, 0);
            lokace.getBlock().setType(AIR);
        }
        //strop
        //strop
        //strop
        lokace.add(0, 2, -5);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(10, 0, 1);
        for (int i = 1; i < 11; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }

        //strop
        //strop
        //strop
        //vybaveni
        //vybaveni
        //vybaveni
        lokace.add(5, -3, -1);
        lokace.getBlock().setType(CHEST);
        lokace.add(-4, 1, 0);
        lokace.getBlock().setType(TORCH);
        lokace.add(0, 0, -8);
        lokace.getBlock().setType(TORCH);
        lokace.add(8, 0, 0);
        lokace.getBlock().setType(TORCH);
        lokace.add(0, 0, 8);
        lokace.getBlock().setType(TORCH);
        lokace.add(-5, -1, 0);
        lokace.getBlock().setType(FURNACE);
        //vybaveni
        //vybaveni
        //vybaveni
        //strecha
        //strecha
        //strecha
        lokace.add(4, 4, 0);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 9; i++) {
            lokace.add(0, 0, -1);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 8; i++) {
            lokace.add(1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 9; i++) {
            lokace.add(0, 0, 1);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, 1, -1);
        for (int i = 1; i < 7; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 7; i++) {
            lokace.add(0, 0, -1);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 6; i++) {
            lokace.add(1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 7; i++) {
            lokace.add(0, 0, 1);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, 1, -1);
        for (int i = 1; i < 5; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 5; i++) {
            lokace.add(0, 0, -1);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 4; i++) {
            lokace.add(1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 5; i++) {
            lokace.add(0, 0, 1);
            lokace.getBlock().setType(BRICKS);
        }
        lokace.add(0, 1, -1);
        for (int i = 1; i < 3; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 3; i++) {
            lokace.add(0, 0, -1);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 2; i++) {
            lokace.add(1, 0, 0);
            lokace.getBlock().setType(BRICKS);
        }
        for (int i = 1; i < 3; i++) {
            lokace.add(0, 0, 1);
            lokace.getBlock().setType(BRICKS);
        }
        //strecha
        //strecha
        //strecha
        //strop pryc
        //strop pryc
        //strop pryc
        lokace.add(4, -4, -4);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }
        lokace.add(7, 0, 1);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }
        lokace.add(7, 0, 1);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }
        lokace.add(7, 0, 1);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }
        lokace.add(7, 0, 1);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }
        lokace.add(7, 0, 1);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }
        lokace.add(7, 0, 1);
        for (int i = 1; i < 8; i++) {
            lokace.add(-1, 0, 0);
            lokace.getBlock().setType(AIR);
        }


        //strop pryc
        //strop pryc
        //strop pryc
        //vybaveni 2
        //vybaveni 2
        //vybaveni 2
        lokace.add(4, -3, 1);
        lokace.getBlock().setType(IRON_BLOCK);
        lokace.add(0, 1, 0);
        lokace.getBlock().setType(IRON_BLOCK);
        lokace.add(0, 0, -1);
        lokace.getBlock().setType(STONE_BUTTON);
        lokace.add(0, -1, 0);
        lokace.getBlock().setType(STONE_BUTTON);
        lokace.add(1, 0, 1);
        lokace.getBlock().setType(CRAFTING_TABLE);
    }
}

